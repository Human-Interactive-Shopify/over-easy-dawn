<section class="FindUs section-{{ section.id }}" data-section-name="Find Us">
  {% if section.settings.show_top_wave %}
    {% render 'waves-top', wave_color: section.settings.top_wave_color %}
   {% endif %}
  <div class="FindUs-wrapper">
    {% comment %} <h2 class="desktop">&#9135;&#9135;&#9135;&#9135; &nbsp; Find us here &nbsp; &#9135;&#9135;&#9135;&#9135;</h2>
    <h2 class="mobile">&#9135;&#9135;&#9135;&#9135; &nbsp; Find us here! &nbsp; &#9135;&#9135;&#9135;&#9135;</h2> {% endcomment %}
    <div class="FindUs-scroller">
      <div class="FindUs-outer-wrap">
        {% for block in section.blocks %}
          
         {{ block.settings.merchant_image.src | image_url: width: 400 | image_tag:
            loading: 'lazy',
            width: block.settings.merchant_image.width,
            height: block.settings.merchant_image.height,
            alt: block.settings.collection.image.alt | escape
          }}

        {% endfor %}
        {% for block in section.blocks %}
          
          {{ block.settings.merchant_image.src | image_url: width: 400 | image_tag:
             loading: 'lazy',
             width: block.settings.merchant_image.width,
             height: block.settings.merchant_image.height,
             alt: block.settings.collection.image.alt | escape
           }}
          
        {% endfor %}
      </div>
    </div>
    <a href="/pages/stores" class="button button--primary button-size--big button-color--black">Store locator</a>
  </div>
  {% if section.settings.show_bottom_wave %}
    {% render 'waves-bottom', wave_color: section.settings.bottom_wave_color %}
  {% endif %}
</section>

<style>
  .FindUs {
    position: relative;
  }
  .FindUs-wrapper {
    margin: 0 auto;
    text-align: center;
    padding: 60px 0;
    overflow-x: hidden;
  }
  .FindUs-wrapper > h2 {
    font-family: Gopher;
}
  .FindUs-wrapper > h2.desktop {
    display:none;
}
.FindUs-wrapper > h2.mobile {
  display: block;
  font-size: 16px;
  text-align: center;
  margin-bottom: 24px;
}
.FindUs-scroller {
  display: inline-block;
  animation: move 12s linear infinite;
  margin-bottom: 24px;
}
.FindUs-outer-wrap {
  display:flex;
  align-content: center;
  align-items: center;
  gap: 80px;
}
@keyframes move {
  from {transform: translateX(0%);}
  to { transform: translateX(calc(-50%)); }
}
.FindUs-inner-wrap{
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  margin: 0 40px;
}

.FindUs-img-wrap {
  width: 190px;
  height: 100px;
}

.FindUs-img-wrap.is-small {
  width: 130px;
  height: 80px;
}

.FindUs-img-wrap img {
  width: 100%;
  object-fit: contain;
}

@media (min-width: 768px) {
  .FindUs-scroller {
    margin-bottom: 32px;
    animation: move 36s linear infinite;
  }
  .FindUs-inner-wrap{
    height: 150px;
    flex-direction: row;
  }
  .FindUs-img-wrap {
    padding-bottom: 0;
    border: none;
  }

}

@media (min-width: 1024px) {
  .FindUs-wrapper {
  }
  .FindUs-wrapper > h2.mobile {
    display:none;
  }
  .FindUs-wrapper > h2.desktop {
    display: block;
    font-size: 16px;
    text-align: center;
    margin-bottom: 32px;
  }
}

</style>


{% schema %}
  {
    "name": "OE Find Us",
    "settings": [
      {
      "type": "checkbox",
      "id": "show_top_wave",
      "label": "Show top wave",
      "default": false
    },
    {
      "type": "select",
      "id": "top_wave_color",
      "options": [
        {
          "value": "white",
          "label": "White"
        },
        {
          "value": "oat",
          "label": "Oat"
        },
        {
          "value": "yellow",
          "label": "Yellow"
        }
      ],
      "default": "white",
      "label": "Top wave color"
    },
    {
      "type": "checkbox",
      "id": "show_bottom_wave",
      "label": "Show bottom wave",
      "default": false
    },
    {
      "type": "select",
      "id": "bottom_wave_color",
      "options": [
        {
          "value": "white",
          "label": "White"
        },
        {
          "value": "oat",
          "label": "Oat"
        },
        {
          "value": "yellow",
          "label": "Yellow"
        }
      ],
      "default": "white",
      "label": "Bottom wave color"
    }
    ],
    "blocks": [
      {
        "name": "OE Find Us",
        "type": "merchant",
        "settings": [
          {
            "type": "image_picker",
            "id": "merchant_image",
            "label": "Merchant Image"
          },
          {
            "type": "checkbox",
            "id": "is_small",
            "label": "Use smaller image"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "OE Find Us",
        "category": "Layout"
      }
    ]
  }
{% endschema %}
